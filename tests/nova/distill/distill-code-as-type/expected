distilled 1 modules (6 items) to out
elided 0 of 0 ∈-annotations and motives, blanked 0 arguments
round-trip OK: ASTs identical, kernel Σ α-identical.
-- distill golden: a CODE standing as a type is bracketed by the TYPE
-- grammar (T{2}), whose spine takes the ELEMENT spine's steps — an
-- argument, an implicit override, the no-insert marker, a projection.
-- All of those print bare; what parenthesizes is what the element
-- ladder puts ABOVE a spine, and the parens re-enter the element
-- grammar whole

infixl 4 ≤
def P : 𝕌 × 𝕌 ≔ ℕ, 𝟙

-- both written parenthesized; the spine reads them back bare
def f : P .π₁ → P .π₁ ≔ λx. x

def Vec : {a : 𝕌} → ℕ → 𝕌 ≔ λa. λn. ℕ

def g : Vec {ℕ} 3 → ℕ using (Vec.eq) ≔ λx. x

-- an operator application is above the spine, so its parentheses stay
def ≤ : ℕ → ℕ → 𝕌 ≔ λa. λb. ℕ

def h : (Z ≤ Z) → ℕ using (≤.eq) ≔ λx. x
