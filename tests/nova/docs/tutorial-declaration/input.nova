def plus : ℕ → ℕ → ℕ
  | plus Z n ≔ n
  | plus (S m) n ≔ S (plus m n)

def double : ℕ → ℕ
  | double n ≔ plus n n

def plusZr : (n : ℕ) → plus n Z ≡ n

def doubleZ : double Z ≡ Z using (plusZr, double.eq) ≔ ⋆
