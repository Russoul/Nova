-- nf of a definition unfolds signature references and fully
-- resolves a ℕ-elim chain (2 + 2 = 4)
def plus : ℕ → ℕ → ℕ ≔ λx. λy. ℕ-elim (n. ℕ) x (n ih. S ih) y
def four : ℕ ≔ plus (S (S Z)) (S (S Z))
