-- The common case: nothing after the eliminated variable depends on
-- it, so the motive is the one the elaborator RECOVERS and none is
-- written — the canonical spelling the distiller elides to anyway
-- (docs/NovaElaboration.txt, In-place elimination).
def f : (n : ℕ) → ℕ ≔ λn. ?goal
