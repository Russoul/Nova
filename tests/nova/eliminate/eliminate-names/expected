def f : (n : ℕ) → ℕ ≔ λn. (ℕ-elim ?goalZ (k rec. ?goalS) n)
def f : (n : ℕ) → ℕ ≔ λn. (ℕ-elim ?base (n ih. ?step) n)
def p : (x : ℕ × ℕ) → P (x .π₁) ≔ λx. (let fst ≔ x .π₁ in
                                       let snd ≔ x .π₂ in
                                       (?g1 : (P fst)))
--- a safe rename of the closed hypothesis:
def c : (m : ℕ) → (n : ℕ) → (h : P n → P m) → ℕ ≔ λm. λn. λh. ((ℕ-elim (n. (hyp : (P n) → (P m)) → ℕ)
                                                               (λhyp. ?g2Z)
                                                               (n ih. λhyp. ?g2S)
                                                               n) h)
--- and one that would capture:
input.nova:15:63: a generated binder would capture m, which this goal mentions — name it something else
--- a name that is not in the context:
input.nova:10:39: nosuch is not a variable of this goal's context
--- a variable with no eliminator and no equation:
input.nova:19:31: g : this type has no eliminator, and no equation of the context has it as a side
