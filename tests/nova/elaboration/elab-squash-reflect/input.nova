-- squashed reflection: a squashed equality hypothesis reflects into a
-- judgemental equality (el-squash-e-eq + el-reflect), so Refl checks
def sr : (a : ℕ) (b : ℕ) (h : Prf ∥a ≡ b ∈ ℕ∥) → a ≡ b ∈ ℕ ≔ λa. λb. λh. Refl
