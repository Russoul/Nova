def cst : (A B : 𝕌) → El A → El B → El A ≔ λA. λB. λa. λb. a

-- the second blank has no source: B occurs only at the b position,
-- and b itself is the blank — a structural error naming the remedy
def bad : El ℕ → El ℕ ≔ λn. cst _ _ n
