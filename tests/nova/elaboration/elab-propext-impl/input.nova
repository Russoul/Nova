-- code-prop-eq where nothing is evident: the two implications are
-- supplied as a pair (e-star-propext). No search could find them —
-- at opaque p and q there is nothing to compute.
def propExt : (p : Ω) (q : Ω) → (p → q) → (q → p) → p ≡ q ∈ Ω ≔
  λp. λq. λf. λg. ⋆ (f , g)
