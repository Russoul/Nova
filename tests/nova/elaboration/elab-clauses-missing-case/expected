declared pred and its equations (pred, predS, predEta) — clauses outside the structural fragment [+3 declarations]
open declarations (3):
  [pred] ⊢ ? : ℕ → ℕ
      at: def pred
  [predS] ⊢ ? : (n:ℕ) → (pred (S n) ≡ n ∈ ℕ)
      at: def predS
  [predEta] ⊢ ? : (x:ℕ → ℕ) → ((n:ℕ) → (x (S n) ≡ n ∈ ℕ)) → (n:ℕ) → (x n ≡ pred n ∈ ℕ)
      at: def predEta
