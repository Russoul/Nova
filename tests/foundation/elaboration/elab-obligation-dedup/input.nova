def plus : ℕ → ℕ → ℕ ≔ λx. λy. ℕ-elim (n. ℕ) x (n ih. S ih) y
def one (n : ℕ) : plus[] Z n ≡ n ∈ ℕ ≔ Refl
-- the same equation again: deduplicated, reported once
def two (n : ℕ) : plus[] Z n ≡ n ∈ ℕ ≔ Refl
