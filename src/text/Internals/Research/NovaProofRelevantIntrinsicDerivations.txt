////////// Preface //////////

This file contains a formalisation of Nova's type theory: Extensional Type Theory.
We have clear distinction between syntax and derivations. In general, a derivation can't be recovered from the syntax alone.


//////////// Type Theory /////////////

We have:
 * Empty type: 𝟘
 * Unit type: 𝟙
 * Natural numbers type: ℕ
 * Dependent product type: Π
 * Dependent sum type: Σ
 * Identity type with reflection: Id
 * A universe: 𝕌

/////// Judgement forms /////////

Σ ∈ sig


Σ₀ ∈ sig
Σ₁ ∈ sig
----------------
Σ⁼ ∈ Σ₀ ≡ Σ₁ sig


Σ ∈ sig
-------------
Σ ⊦ E ∈ entry


Σ⁼ ∈ Σ₀ ≡ Σ₁ sig
Σ₀ ⊦ E₀ ∈ entry
Σ₁ ⊦ E₁ ∈ entry
-----------------------
Σ⁼ ⊦ E⁼ ∈ E₀ ≡ E₁ entry


Σ⁼ ⊦ E⁼ ∈ E₀ ≡ E₁
Σ₀ ⊦ e₀ : E₀
Σ₁ ⊦ e₁ : E₁
------------------------
Σ⁼ ⊦ e⁼ ∈ (e₀ ≡ e₁ : E⁼)


Σ ∈ sig
ϴ ∈ sig
---------
σ ∈ Σ ⇒ ϴ


Σ⁼ ∈ Σ₀ ≡ Σ₁
ϴ⁼ ∈ ϴ₀ ≡ ϴ₁
σ₀ ∈ Σ₀ ⇒ ϴ₀
σ₁ ∈ Σ₁ ⇒ ϴ₁
------------------------
σ⁼ ∈ (σ₀ ≡ σ₁ : Σ⁼ ⇒ ϴ⁼)


Σ sig
---------
Σ ⊦ Γ ctx


Σ₀ ⊦ Γ₀ ctx
Σ₁ ⊦ Γ₁ ctx
Σ⁼ ∈ Σ₀ ≡ Σ₁
-----------------
Σ⁼ ⊦ Γ⁼ ∈ Γ₀ ≡ Γ₁


Σ⁼ ⊦ Γ⁼ ∈ Γ₀ ≡ Γ₁
Σ⁼ ⊦ Δ⁼ ∈ Δ₀ ≡ Δ₁
-----------------------------
Σ⁼ ⊦ σ⁼ ∈ (σ₀ ≡ σ₁ : Γ⁼ ⇒ Δ⁼)


Σ⁼ ⊦ Γ⁼ ∈ Γ₀ ≡ Γ₁
Σ₀ Γ₀ ⊦ A₀ type
Σ₁ Γ₁ ⊦ A₁ type
--------------------
Σ⁼ Γ⁼ ⊦ A⁼ ∈ A₀ ≡ A₁


Σ⁼ ⊦ Γ⁼ ∈ Γ₀ ≡ Γ₁
---------------------
Σ⁼ Γ⁼ ⊦ Δ⁼ ∈ Δ₀ ≡ Δ₁


Σ⁼ Γ⁼ ⊦ A⁼ ∈ A₀ ≡ A₁
Σ₀ Γ₀ ⊦ a₀ ∈ A₀
Σ₁ Γ₁ ⊦ a₁ ∈ A₁
---------------------------
Σ⁼ Γ⁼ ⊦ a⁼ ∈ (a₀ ≡ a₁ : A⁼)


Σ⁼ Γ⁼ ⊦ Δ⁼ ∈ Δ₀ ≡ Δ₁
---------------------------
Σ⁼ Γ⁼ ⊦ ē⁼ ∈ (ē₀ ≡ ē₁ : Δ⁼)


///////// Inference rules (sig) //////////

ε ∈ sig


ε⁼ ∈ ε ≡ ε sig


Σ ∈ sig
Σ ⊦ E ∈ entry
-------------
Σ E ∈ sig


Σ⁼₀₁ ∈ Σ₀ ≡ Σ₁ sig
Σ⁼₁₂ ∈ Σ₁ ≡ Σ₂ sig
-------------------------
Σ⁼₀₁ · Σ⁼₁₂ ∈ Σ₀ ≡ Σ₂ sig


Σ⁼ ∈ Σ₀ ≡ Σ₁ sig
Σ⁼ ⊦ E⁼ ∈ E₀ ≡ E₁ entry
-------------------------
Σ⁼ E⁼ ∈ Σ₀ E₀ ≡ Σ₁ E₁ sig


Σ⁼₀₁ ⊦ E⁼₀₁ ∈ E₀ ≡ E₁ entry
Σ⁼₁₂ ⊦ E⁼₁₂ ∈ E₁ ≡ E₂ entry
-----------------------------------------
Σ⁼₀₁ · Σ⁼₁₂ ⊦ E⁼₀₁ · E⁼₁₂ ∈ E₀ ≡ E₂ entry

///////// Inference rules (sig entry) //////////

Σ⁼ ∈ Σ₀ ≡ Σ₁ sig
Σ⁼ ⊦ Γ⁼ ∈ Γ₀ ≡ Γ₁ ctx
--------------------------------------------------
Σ⁼ ⊦ (Γ⁼ ⊦ type) ∈ (Γ₀ ⊦ type) ≡ (Γ₁ ⊦ type) entry


Σ⁼ ∈ Σ₀ ≡ Σ₁ sig
Σ⁼ ⊦ Γ⁼ ∈ Γ₀ ≡ Γ₁ ctx
Σ⁼ Γ⁼ ⊦ A⁼ ∈ A₀ ≡ A₁ type
--------------------------------------------
Σ⁼ ⊦ (Γ⁼ ⊦ A⁼) ∈ (Γ₀ ⊦ A₀) ≡ (Γ₁ ⊦ A₁) entry



Σ⁼ ⊦ E⁼ ∈ E₀ ≡ E₁ entry
σ⁼ ∈ (σ₀ ≡ σ₁ : Ξ⁼ ⇒ Σ⁼)
-----------------------------------
Ξ⁼ ⊦ E⁼[σ⁼] ∈ E₀[σ₀] ≡ E₁[σ₁] entry


///////// Inference rules (sig substitution) //////////

· ≡ · : Σ₀ ≡ Σ₁ ⇒ ε ≡ ε

σ₀ ≡ σ₁ : Σ₀ ≡ Σ₁ ⇒ ϴ₀ ≡ ϴ₁
ϴ₀ ≡ ϴ₁ ⊦ E₀ ≡ E₁ entry
Σ₀ ≡ Σ₁ ⊦ e₀ ≡ e₁ : E₀(σ₀) ≡ E₁(σ₁)
---------------------------------------------
(σ₀, e₀) ≡ (σ₁, e₁) : Σ₀ ≡ Σ₁ ⇒ ϴ₀ E₀ ≡ ϴ₁ E₁

τ₀ ≡ τ₁ : Σ₀ ≡ Σ₁ ⇒ ϴ₀ ≡ ϴ₁
σ₀ ≡ σ₁ : ϴ₀ ≡ ϴ₁ ⇒ Ξ₀ ≡ Ξ₁
-----------------------------------------
(σ₀ ∘ τ₀) ≡ (σ₁ ∘ τ₁) : Σ₀ ≡ Σ₁ ⇒ Ξ₀ ≡ Ξ₁

Σ₀ ≡ Σ₁ ⊦ E₀ ≡ Σ₁ entry
-------------------------------
↑ ≡ ↑ : Σ₀ E₀ ≡ Σ₁ E₁ ⇒ Σ₀ ≡ Σ₁

Σ₀ ≡ Σ₁ sig
---------------------------
id ≡ id : Σ₀ ≡ Σ₁ ⇒ Σ₀ ≡ Σ₁

σ₀ ≡ σ₁ : Σ₀ ≡ Σ₁ ⇒ ε ≡ ε
-------------------------
σ₀ ≡ · : Σ₀ ≡ Σ₁ ⇒ ε ≡ ε

σ₀ ≡ σ₁ : Σ₀ ≡ Σ₁ ⇒ ε ≡ ε
-------------------------
σ₁ ≡ · : Σ₀ ≡ Σ₁ ⇒ ε ≡ ε

σ₀ ≡ σ₁ : Σ₀ ≡ Σ₁ ⇒ ϴ₀ ≡ ϴ₁
---------------------------
id ∘ σ₀ ≡ σ₀ : Σ₀ ≡ Σ₁ ⇒ ϴ₀ ≡ ϴ₁

σ₀ ≡ σ₁ : Σ₀ ≡ Σ₁ ⇒ ϴ₀ ≡ ϴ₁
---------------------------
id ∘ σ₁ ≡ σ₁ : Σ₀ ≡ Σ₁ ⇒ ϴ₀ ≡ ϴ₁

σ₀ ≡ σ₁ : Σ₀ ≡ Σ₁ ⇒ ϴ₀ ≡ ϴ₁
---------------------------
σ₀ ∘ id ≡ σ₀ : Σ₀ ≡ Σ₁ ⇒ ϴ₀ ≡ ϴ₁

σ₀ ≡ σ₁ : Σ₀ ≡ Σ₁ ⇒ ϴ₀ ≡ ϴ₁
--------------------------------
σ₁ ∘ id ≡ σ₁ : Σ₀ ≡ Σ₁ ⇒ ϴ₀ ≡ ϴ₁

σ₁₀ : Σ₁ ⇒ Σ₀
σ₂₁ : Σ₂ ⇒ Σ₁
σ₃₂ : Σ₃ ⇒ Σ₂
-----------------------------------------------
σ₁₀ ∘ (σ₂₁ ∘ σ₃₂) ≡ (σ₁₀ ∘ σ₂₁) ∘ σ₃₂ : Σ₃ ⇒ Σ₀

σ : Σ ⇒ ϴ
ϴ ⊦ E entry
Σ ⊦ e : E(σ)
----------------------------
↑ ∘ (σ, e) ≡ σ : Σ ⇒ ϴ

τ : Σ₀ ⇒ Σ₁
σ : Σ₁ ⇒ Σ₂
Σ₂ ⊦ E entry
Σ₁ ⊦ e : E(σ)
--------------------------------------------
(σ, e) ∘ τ ≡ (σ ∘ τ, e(τ)) : Σ₀ ⇒ Σ₂ E


///////// Inference rules (ctx) //////////

Σ₀ ≡ Σ₁ sig
-------------------
Σ₀ ≡ Σ₁ ⊦ ε ≡ ε ctx

(Σ₀ ≡ Σ₁) (Γ₀ ≡ Γ₁) ⊦ A₀ ≡ A₁ type
----------------------------------
Σ₀ ≡ Σ₁ ⊦ Γ₀ A₀ ≡ Γ₁ A₁ ctx

σ₀ ≡ σ₁ : Σ₀ ≡ Σ₁ ⇒ ϴ₀ ≡ ϴ₁
ϴ₀ ≡ ϴ₁ ⊦ Γ₀ ≡ Γ₁ ctx
------------------------------------
Σ₀ ≡ Σ₁ ⊦ Γ₀(σ₀) ≡ Γ₁(σ₁) ctx, where

     ε(σ) ≔ ε                  // by induction on syntax of context
     (Γ A)(σ) ≔ Γ(σ) A(σ)

///////// Inference rules (ctx substitution) //////////

Σ₀ ≡ Σ₁ ⊦ Γ₀ ≡ Γ₁ ctx
---------------------------------
Σ₀ ≡ Σ₁ ⊦ · ≡ · : Γ₀ ≡ Γ₁ ⇒ ε ≡ ε

Σ₀ ≡ Σ₁ ⊦ Γ₀ ≡ Γ₁ ctx
--------------------------------
Σ₀ ≡ Σ₁ ⊦ id : Γ₀ ≡ Γ₁ ⇒ Γ₀ ≡ Γ₁

Σ₀ ≡ Σ₁ ⊦ Γ₀ A₀ ≡ Γ₁ A₁ ctx
-----------------------------------------
Σ₀ ≡ Σ₁ ⊦ ↑ ≡ ↑ : Γ₀ A₀ ≡ Γ₁ A₁ ⇒ Γ₀ ≡ Γ₁

Σ₀ ≡ Σ₁ ⊦ σ₀ ≡ σ₁ : Δ₀ ≡ Δ₁ ⇒ Γ₀ ≡ Γ₁
Σ₀ ≡ Σ₁ ⊦ τ₀ ≡ τ₁ : Ξ₀ ≡ Ξ₁ ⇒ Δ₀ ≡ Δ₁
-------------------------------------------
Σ₀ ≡ Σ₁ ⊦ σ₀ ∘ τ₀ ≡ σ₁ ∘ τ₁ ≡ : Ξ₀ ≡ Ξ₁ ⇒ Γ₀ ≡ Γ₁

Σ₀ ≡ Σ₁ ⊦ σ₀ ≡ σ₁ : Δ₀ ≡ Δ₁ ⇒ Γ₀ ≡ Γ₁
(Σ₀ ≡ Σ₁) (Γ₀ ≡ Γ₁) ⊦ A₀ ≡ A₁ type
(Σ₀ ≡ Σ₁) (Δ₀ ≡ Γ₁) ⊦ t₀ ≡ t₁ : A₀(σ₀) ≡ A₁(σ₁)
---------------------------------------------------
Σ₀ ≡ Σ₁ ⊦ σ₀, t₀ ≡ σ₁, t₁ : Δ₀ ≡ Δ₁ ⇒ Γ₀ A₀ ≡ Γ₁ A₁

σ₀ ≡ σ₁ : Σ₀ ≡ Σ₁ ⇒ ϴ₀ ≡ ϴ₁
ϴ₀ ≡ ϴ₁ ⊦ τ₀ ≡ τ₁ : Γ₀ ≡ Γ₁ ⇒ Δ₀ ≡ Δ₁
-------------------------------------------------------------
Σ₀ ≡ Σ₁ ⊦ τ₀(σ₀) ≡ τ₁(σ₁) : Γ₀(σ₀) ≡ Γ₁(σ₁) ⇒ Δ₀(σ₀) ≡ Δ₁(σ₁)

Σ ⊦ Γ ctx
Σ ⊦ σ : Γ ⇒ ε
-----------------
Σ ⊦ σ = · : Γ ⇒ ε

Σ ⊦ σ : Γ ⇒ Δ
----------------------
Σ ⊦ σ ∘ id = σ : Γ ⇒ Δ

Σ ⊦ σ : Γ ⇒ Δ
----------------------
Σ ⊦ id ∘ σ = σ : Γ ⇒ Δ

Σ ⊦ σ₁₀ : Γ₁ ⇒ Γ₀
Σ ⊦ σ₂₁ : Γ₂ ⇒ Γ₁
Σ ⊦ σ₃₂ : Γ₃ ⇒ Γ₂
---------------------------------------------------
Σ ⊦ σ₁₀ ∘ (σ₂₁ ∘ σ₃₂) = (σ₁₀ ∘ σ₂₁) ∘ σ₃₂ : Γ₃ ⇒ Γ₀

Σ ⊦ σ : Δ ⇒ Γ
Σ Γ ⊦ A type
Σ Δ ⊦ t : A(σ)
--------------------------
Σ ⊦ ↑ ∘ (σ, t) = σ : Δ ⇒ Γ

Σ ⊦ τ : Γ₀ ⇒ Γ₁
Σ ⊦ σ : Γ₁ ⇒ Γ₂
Σ Γ₂ ⊦ A type
Σ Γ₁ ⊦ t : A(σ)
------------------------------------------
Σ ⊦ (σ, t) ∘ τ = (σ ∘ τ, t(τ)) : Γ₀ ⇒ Γ₂ A

///////// Inference rules (type) //////////

Σ Γ ⊦ 𝟘 type

Σ Γ ⊦ 𝟙 type

Σ Γ ⊦ ℕ type

Σ Γ ⊦ 𝕌 type

Σ Γ ⊦ A : 𝕌
---------------
Σ Γ ⊦ El A type

(Σ₀ ≡ Σ₁) (Γ₀ A₀ ≡ Γ₁ A₁) ⊦ B₀ ≡ B₁ type
--------------------------------------------
(Σ₀ ≡ Σ₁) (Γ₀ ≡ Γ₁) ⊦ A₀ → B₀ ≡ A₀ → B₀ type

Σ Γ ⊦ A type
Σ Γ A ⊦ B type
----------------
Σ Γ ⊦ A ⨯ B type

(Σ₀ ≡ Σ₁) (Γ₀ ≡ Γ₁) ⊦ a₀ ≡ a₁ : A₀ ≡ A₁
(Σ₀ ≡ Σ₁) (Γ₀ ≡ Γ₁) ⊦ b₀ ≡ b₁ : B₀ ≡ B₁
------------------------------------------------------------------------
(Σ₀ ≡ Σ₁) (Γ₀ ≡ Γ₁) ⊦ (Id a₀ b₀ ∈ Id A₀ B₀) ≡ (Id a₁ b₁ ∈ Id A₁ B₁) type

Σ₁ Γ ⊦ A type
σ : Σ₀ ⇒ Σ₁
-------------------
Σ₀ Γ[σ] ⊦ A[σ] type

Σ Γ ⊦ A type
Σ ⊦ σ : Δ ⇒ Γ
---------------
Σ Δ ⊦ A(σ) type

Σ Γ ⊦ El 𝟘 = ℕ type

Σ Γ ⊦ El 𝟙 = ℕ type

Σ Γ ⊦ El ℕ = ℕ type

Σ Γ ⊦ El 𝕌 = 𝕌 type

Σ Γ ⊦ A : 𝕌
Σ Γ (El A) ⊦ B : 𝕌
-----------------------------------
Σ Γ ⊦ El (A → B) = (El A → El B) type

Σ Γ ⊦ A : 𝕌
Σ Γ (El A) ⊦ B : 𝕌
-----------------------------------
Σ Γ ⊦ El (A ⨯ B) = (El A ⨯ El B) type

Σ Γ ⊦ A : 𝕌
Σ Γ ⊦ a₀ : El A
Σ Γ ⊦ a₁ : El A
---------------------------------------------
Σ Γ ⊦ El(a₀ ≡ a₁ ∈ A) = (a₀ ≡ a₁ ∈ El A) type

Σ ⊦ σ : Δ ⇒ Γ
---------------------
Σ Δ ⊦ 𝟘(σ) = ℕ type

Σ ⊦ σ : Δ ⇒ Γ
---------------------
Σ Δ ⊦ 𝟙(σ) = ℕ type

Σ ⊦ σ : Δ ⇒ Γ
---------------------
Σ Δ ⊦ ℕ(σ) = ℕ type

Σ ⊦ σ : Δ ⇒ Γ
---------------------
Σ Δ ⊦ 𝕌(σ) = 𝕌 type

Σ ⊦ σ : Δ ⇒ Γ
Σ Γ ⊦ A type
---------------------------------
Σ ⊦ σ⁺ : Δ A(σ) ⇒ Γ A
    σ⁺ ≔ σ ∘ ↑, x₀

Σ ⊦ σ : Δ ⇒ Γ
------------------------------------
Σ Δ ⊦ (A → B)(σ) = A → B(σ⁺(A)) type

Σ ⊦ σ : Δ ⇒ Γ
------------------------------------
Σ Δ ⊦ (A ⨯ B)(σ) = A ⨯ B(σ⁺(A)) type

Σ ⊦ σ : Δ ⇒ Γ
----------------------------------------------------
Σ Δ ⊦ (a₀ ≡ a₁ ∈ A)(σ) = (a₀(σ) ≡ a₁(σ) ∈ A(σ)) type

Σ ⊦ σ : Δ ⇒ Γ
Σ ⊦ τ : Ξ ⇒ Δ
-------------------------------
Σ Ξ ⊦ A(σ)(τ) = A(σ ∘ τ) type

(Σ₀ (Γ₀ ⊦ type) ≡ Σ₁ (Γ₁ ⊦ type)) (Γ₀[↑] ≡ Γ₁[↑]) ⊦ ◯ ≡ ◯ type

///////// Inference rules (tel) //////////

Σ ⊦ Γ ctx
-----------
Σ Γ ⊦ ε tel

Σ Γ A ⊦ Δ tel
-------------
Σ Γ ⊦ A Δ tel

σ : Σ₀ ⇒ Σ₁
Σ₁ Γ ⊦ Δ tel
------------------  //Defined by meta-level induction
Σ₀ Γ(σ) ⊦ Δ(σ) tel
          ε(σ) = ε
          (A Δ)(σ) = A(σ) Δ(σ)

Σ ⊦ σ : Γ₀ ⇒ Γ₁
Σ Γ₁ ⊦ Δ tel
------------------  //Defined by meta-level induction
Σ Γ₀ ⊦ Δ(σ) tel
       ε(σ) = ε
       (A Δ)(σ) = A(σ) Δ(σ⁺)

///////// Inference rules (elem) //////////

Σ Γ ⊦ 𝟘 : 𝕌

Σ Γ ⊦ 𝟙 : 𝕌

Σ Γ ⊦ ℕ : 𝕌

Σ Γ ⊦ () : 𝟙

Σ Γ ⊦ Z : ℕ

Σ Γ ⊦ t : ℕ
-------------
Σ Γ ⊦ S t : ℕ

Σ Γ ⊦ A : 𝕌
Σ Γ (El A) ⊦ B : 𝕌
----------------------
Σ Γ ⊦ A → B : 𝕌

Σ Γ ⊦ A : 𝕌
Σ Γ (El A) ⊦ B : 𝕌
----------------------
Σ Γ ⊦ A ⨯ B : 𝕌

Σ Γ ⊦ A : 𝕌
Σ Γ ⊦ a₀ : El A
Σ Γ ⊦ a₁ : El A
-----------------------
Σ Γ ⊦ (a₀ ≡ a₁ ∈ A) : 𝕌

(Σ₀ ≡ Σ₁) (Γ₀ A₀ ≡ Γ₁ A₁) ⊦ f₀ ≡ f₁ : B₀ ≡ B₁
-----------------------------------------------------
(Σ₀ ≡ Σ₁) (Γ₀ ≡ Γ₁) ⊦ λ f₀ ≡ λ f₁ : A₀ → B₀ ≡ A₁ → B₁

Σ Γ ⊦ f : A → B
Σ Γ ⊦ e : A
--------------------
Σ Γ ⊦ f e : B(id, e)

Σ Γ ⊦ a : A
Σ Γ ⊦ b : B(id, a)
----------------------------
Σ Γ ⊦ a , b : A ⨯ B

Σ Γ ⊦ t : A ⨯ B
---------------
Σ Γ ⊦ t .π₁ : A

Σ Γ ⊦ t : A ⨯ B
------------------------------------
Σ Γ ⊦ t .π₂ : B(id, (t : A ⨯ B) .π₁)

(Σ₀ ≡ Σ₁) (Γ₀ ≡ Γ₁) ⊦ a₀ ≡ a₁ : A₀ ≡ A₁
-----------------------------------------------------
Σ Γ ⊦ Refl ≡ Refl : (Id a₀ a₀ ∈ A₀) ≡ (Id a₁ a₁ ∈ A₁)

------------------------
Σ Γ (x : 𝟘) ⊦ 𝟘-elim : A

(Σ₀ ≡ Σ₁) (Γ₀ ℕ ≡ Γ₁ ℕ) ⊦ A₀ ≡ A₁ type
(Σ₀ ≡ Σ₁) (Γ₀ ≡ Γ₁) ⊦ z₀ ≡ z₁ : A₀(id, Z) ≡ A₁(id, Z)
(Σ₀ ≡ Σ₁) (Γ₀ ℕ ≡ Γ₁ ℕ) ⊦ s₀ ≡ s₁ : A₀(↑, S ☐) ≡ A₁(↑, S ☐)
(Σ₀ ≡ Σ₁) (Γ₀ ≡ Γ₁) ⊦ t₀ ≡ t₁ : ℕ
---------------------------------------------------------------------------------------
(Σ₀ ≡ Σ₁) (Γ₀ ≡ Γ₁) ⊦ ℕ-elim A₀ z₀ s₀ t₀ ≡ ℕ-elim A₁ z₁ s₁ t₁ : A₀(id, t₀) ≡ A₁(id, t₁)

---------------------------------------------------
(Σ₀ ≡ Σ₁) (Γ₀ A₀ ≡ Γ₁ A₁) ⊦ ☐ ≡ ☐ : A₀(↑) ≡ A₁(↑)

---------------------------------------------------------------------
(Σ₀ (Γ₀ ⊦ A₀) ≡ Σ₁ (Γ₁ ⊦ A₁)) (Γ₀[↑] ≡ Γ₁[↑]) ⊦ ◯ ≡ ◯ : A₀[↑] ≡ A₁[↑]

(Σ₀ ≡ Σ₁) (Γ₀ ≡ Γ₁) ⊦ t₀ ≡ t₁ : A₀ ≡ A₁
Σ₀ ≡ Σ₁ ⊦ σ₀ ≡ σ₁ : Δ₀ ≡ Δ₁ ⇒ Γ₀ ≡ Γ₁
-------------------------------------------------------
(Σ₀ ≡ Σ₁) (Δ₀ ≡ Δ₁) ⊦ t₀(σ₀) ≡ t₁(σ₁) : A₀(σ₀) ≡ A₁(σ₁)

(Σ₀ ≡ Σ₁) (Γ₀ ≡ Γ₁) ⊦ t₀ ≡ t₁ : A₀ ≡ A₁
σ₀ ≡ σ₁ : ϴ₀ ≡ ϴ₁ ⇒ Σ₀ ≡ Σ₁
---------------------------------------------------------------
(ϴ₀ ≡ ϴ₁) (Γ₀[σ₀] ≡ Γ₁[σ₁]) ⊦ t₀[σ₀] ≡ t₁[σ₁] : A₀[σ₀] ≡ A₁[σ₁]

Σ Γ ⊦ t : 𝟙
----------------
Σ Γ ⊦ t = () : 𝟙

Σ Γ A ⊦ f : B
Σ Γ ⊦ e : A
-------------------------------------
Σ Γ ⊦ (λ f) e = f(id, e) : B(id, e)

Σ Γ ⊦ f : A → B
----------------------------
Σ Γ ⊦ λ (f(↑) ☐) = f : A → B

Σ Γ A ⊦ B type
Σ Γ ⊦ a : A
Σ Γ ⊦ b : B(id, a)
------------------------
Σ Γ ⊦ (a, b) .π₁ = a : A

Σ Γ A ⊦ B type
Σ Γ ⊦ a : A
Σ Γ ⊦ b : B(id, a)
-------------------------------
Σ Γ ⊦ (a, b) .π₂ = b : B(id, a)

Σ Γ ⊦ t : A ⨯ B
---------------------------------
Σ Γ ⊦ (t .π₁ , t .π₂) = t : A ⨯ B

Σ Γ ⊦ A ctx
Σ ⊦ σ : Δ ⇒ Γ
Σ Δ ⊦ t : A(σ)
------------------------
Σ Δ ⊦ ☐(σ, t) = t : A(σ)

σ : Σ₀ ⇒ Σ₁
Σ₀ ⊦ t : (Γ[σ] ⊦ A[σ])
-----------------------------
Σ₀ Γ[σ] ⊦ ◯(σ, t) = t : A[σ]

Σ ⊦ σ : Δ ⇒ Γ
Σ ⊦ τ : Ξ ⇒ Δ
Σ Γ ⊦ A type
-----------------------------------
Σ Ξ ⊦ t(σ)(τ) = t(σ ∘ τ) : A(σ ∘ τ)

Σ Γ (x : ℕ) ⊦ A type
Σ Γ ⊦ z : A(Z / x)
Σ Γ (x : ℕ) (h : A) ⊦ s : A(S x / x)
-----------------------------------------
Σ Γ ⊦ ℕ-elim x.A z x.h.s Z = z : A(Z / x)

Σ Γ (x : ℕ) ⊦ A type
Σ Γ ⊦ z : A(Z / x)
Σ Γ (x : ℕ) (h : A) ⊦ s : A(S x / x)
Σ Γ ⊦ t : ℕ
------------------------------------------------------------------------
Σ Γ ⊦ ℕ-elim x.A z x.h.s (S t) = s(t, ℕ-elim x.A z x.h.s t) : A(S t / x)


Γ ⊦ p : Id a₀ a₁ ∈ A
--------------------
Γ ⊦ a₀ ≡ a₁ : A

// COMMENT: It doesn't look like uniqueness of identity proofs is derivable?

///////// Inference rules (elem list) //////////

Σ Γ ⊦ · : ε

Σ Γ ⊦ Δ tel
Σ Γ ⊦ e : A
Σ Γ ⊦ ē : Δ(e / x)
---------------------
Σ Γ ⊦ e ē : (x : A) Δ

Σ Γ₁ ⊦ Δ tel
Σ Γ₁ ⊦ ē : Δ
Σ ⊦ σ : Γ₀ ⇒ Γ₁
---------------------------------------------- //Defined by meta-level induction
Σ Γ₀ ⊦ ē(σ) : Δ(σ)
       ·(σ) ≔ · : ε
       (e ē)(σ) ≔ e(σ) ē(σ) : (x : A(σ)) Δ(σ⁺)

Σ₁ Γ ⊦ Δ tel
Σ₁ Γ ⊦ ē : Δ
σ : Σ₀ ⇒ Σ₁
------------------------------------------------ //Defined by meta-level induction
Σ₀ Γ(σ) ⊦ ē(σ) : Δ(σ)
          ·(σ) ≔ · : ε
          (e ē)(σ) ≔ e(σ) ē(σ) : (x : A(σ)) Δ(σ)

Σ Γ ⊦ · = · : ε

Σ Γ ⊦ e₀ = e₁ : A
Σ Γ ⊦ ē₀ = ē₁ : Δ(e₀)
-------------------------------
Σ Γ ⊦ e₀ ē₀ = e₁ ē₁ : (x : A) Δ

///////// Inference rules (dsig) //////////

Σ ⊦ ε dsig

Σ ⊦ E entry
Σ (x : E) ⊦ Ξ dsig
------------------
Σ ⊦ (x : E) Ξ dsig

Σ₁ ⊦ Ξ dsig
σ : Σ₀ ⇒ Σ₁
-------------------------------------- //Defined by meta-level induction
Σ₀ ⊦ Ξ(σ) dsig
     ε(σ) = ε
     ((x : E) Ξ)(σ) = (x : E(σ)) Ξ(σ⁺)

///////// Inference rules (dsig substitution) //////////

Σ ⊦ Ξ dsig
-------------
Σ ⊦ · : Ξ ⇒ ε

Σ ⊦ Ξ dsig
--------------
Σ ⊦ id : Ξ ⇒ Ξ

Σ ⊦ ξ₀₁ : Ξ₀ ⇒ Ξ₁
Σ ⊦ ξ₁₂ : Ξ₁ ⇒ Ξ₂
-----------------------
Σ ⊦ ξ₁₂ ∘ ξ₀₁ : Ξ₀ ⇒ Ξ₂

Σ Ξ₀ ⊦ e : E
Σ ⊦ ξ : Ξ₀ ⇒ Ξ₁(e / x)
---------------------------
Σ ⊦ e , ξ : Ξ₀ ⇒ (x : E) Ξ₁

///////// Inference rules (dsig instance) //////////

Σ ⊦ · : ε

Σ ⊦ E entry
Σ (x : E) ⊦ Ξ dsig
Σ ⊦ e : E
Σ ⊦ ē : Ξ(e / x)
-------------------
Σ ⊦ e ē : (x : E) Ξ
