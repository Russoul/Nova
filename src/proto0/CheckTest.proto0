test0 : ℕ

test2 : ℕ → ℕ

test3 : (A : 𝕌) → A

test4 : test0 ≡ test0 ∈ ℕ

id : (A : 𝕌) → A → A

sym ≔ A ↦ a₀ ↦ a₁ ↦ a ↦ J A a₀ (x. e. x ≡ a₀ ∈ A) Refl a₁ a
    : (A : 𝕌) → (a₀ : A) → (a₁ : A) → (a₀ ≡ a₁ ∈ A) → (a₁ ≡ a₀ ∈ A)

fun-eta : (A : 𝕌) → (B : A → 𝕌) → (f : (x : A) → B x) → (f ≡ (x ↦ f x) ∈ (x : A) → B x)

fun-ext : (A : 𝕌) → (B : A → 𝕌) → (f : (x : A) → B x) → (g : (x : A) → B x) → (p : (x : A) → f x ≡ g x ∈ B x) → f ≡ g ∈ (x : A) → B x

-- β A x.B x.f e : (x ↦ f) e ≡ f[e/x] : B[e/x]


{-
test5 : id ℕ Z ≡ id ℕ Z ∈ ℕ -}
