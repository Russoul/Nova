////////// NovaPipeline.txt — the processing pipeline and its trust story //////////

////////// Purpose //////////

This file is the map: what the layers are, what artifact each one
produces, where the trust boundary sits, and which document specifies
each part. It records the architecture converged on after the
elaborator's first implementation exposed a consistency-grade hole in
trusted equality search (see "Why this shape" below); the individual
layers are specified elsewhere:

  * docs/NovaFoundation.txt   — the THEORY. Sole source of truth; every
                                other layer answers to it.
  * docs/NovaKernel.txt       — the KERNEL as implemented, rule by
                                rule: fuel-bounded normalization,
                                certificate replay, item-level
                                checking over skeletons. Slated for
                                demotion to the untrusted
                                RECONSTRUCTOR by the derivation
                                rework (last section below).
  * docs/NovaDerivations.txt  — the kernel rework's TARGET: the
                                derivation format and the replay
                                kernel that checks it.
  * docs/NovaElaboration.txt  — the elaborator: surface syntax,
                                bidirectional rules, the obligation
                                lifecycle.

(The derivation-era machinery — .rules sessions, its checker, parsers
and docs — has been removed; the pipeline below replaced it.)

////////// The pipeline //////////

  .nova source (surface syntax)          — authored: by a human or AI
      │
      │  parse + scope resolution        (pure front end)
      ▼
  indexed surface AST                    — nameless; still carries
      │                                    ascriptions and motives
      │  ELABORATOR                      (untrusted)
      │    bidirectional pass; at each conversion site consults the
      │    DISCHARGE ENGINE (untrusted tactic); records everything it
      │    invents or is handed
      ▼
  per item: ANNOTATED TREE               — the certificate-carrying
      │                                    artifact (see below)
      │  KERNEL                          (trusted, total)
      │    synthesis over the annotated tree + fuel-bounded beta +
      │    certificate replay; no search, no choices, always a verdict
      ▼
  accept / reject                        — the only verdict that counts;
                                           accepted erasures extend the
                                           kernel's Σ (Foundation's Σ,
                                           exactly)

////////// The trust boundary //////////

Everything above the kernel is UNTRUSTED. The elaborator may be
arbitrarily clever; the discharge engine may search, rewrite, and
heuristically match; none of it is believed. The kernel re-establishes
every judgement from its own Σ using only:

  * type synthesis over the annotated tree (annotations supply what
    synthesis cannot invent — see artifact format);
  * FUEL-BOUNDED beta conversion (Foundation's ≜ rules: the beta
    family, El-decoding, signature unfolding) — step budgets come
    from the certificate, exhaustion means
    REJECT, so the kernel is total: every artifact gets a verdict;
  * CERTIFICATE REPLAY: at a conversion site, apply the recorded trace
    steps mechanically — check each step's proof element, rewrite at
    the given path in the given orientation, compare normal forms
    (fuel-bounded normalization between the recorded extensional
    steps).

Consequences of the split:

  * A discharge-engine bug is INCOMPLETENESS (a failed trace → the
    obligation stands), never unsoundness. A bad emitted trace is
    rejected at replay. The engine's soundness is a quality property,
    not a safety property.
  * The same holds for elaborator bugs generally: a wrong core term, a
    mis-substituted type, a bogus motive all die in the kernel.
  * The kernel is small enough to audit against NovaFoundation.txt
    rule by rule, and is the only component with that obligation.

////////// The artifact: annotated trees, not annotated terms //////////

Foundation's core syntax stays BARE — and the kernel, checking
spellings, necessarily works extrinsically even though the theory's
official reading is intrinsic (docs/NovaSyntax.txt): a spelling
inhabits many types at once (a spelling and its El-decoding; an index
and its lemma-equal form), so "the type of a subterm" is not
recoverable from the artifact — only a chosen spelling. Nothing judgmentally inert belongs in
the theory's syntax: no coercion or transport term formers, no type
annotations, no J. (Coercion is a RULE — el-ty-coe — and stays one.
Equality proofs are consumed by reflection and produced as ⋆; the
composition that J/sym/trans/cong would provide inside terms is
provided by trace structure instead: chaining is a list, placement is
a path, symmetry is a flag.)

The kernel's INPUT, however, is richer than a bare term: an ANNOTATED
TREE — the elaborator's output where each node optionally carries
exactly what the bidirectional pass invented or consumed there:

  * eliminator motives (ℕ-elim, quot-elim) — bare core is not even
    re-checkable without them;
  * the expected type at checked introduction forms;
  * conversion traces at switch sites: chains of
    (path, proof element, orientation) steps, plus the quotient-witness
    step kind carrying its witness element;
  * fuel budgets for the kernel's normalization (per item or per site;
    the elaborator knows its own step counts and writes in a margin).

An ERASURE function maps annotated trees to Foundation core terms, and
the kernel invariant is: kernel accepts the annotated tree ⟹ the
erasure is Foundation-derivable at the stated type. Equality,
normalization, the lemma store, and printing all operate on erasures —
the annotation layer is invisible to the theory. The slogan: type
information travels WITH terms in the implementation, and is never OF
terms in the theory.

Caveat recorded once, binding everywhere: an annotation is a
REPRESENTATIVE, not a canonical type — canonicity is unavailable in
principle (spellings are unique only up to a hypothesis-sensitive,
undecidable equality). No consumer may compare annotations
syntactically; only up to conversion.

////////// Computation in the kernel: bounded, not certified //////////

A design alternative was considered and recorded here deliberately:
move beta itself into the certificate (every ≜-step a recorded
(rule, path) entry; the kernel a pure single-step replayer comparing
terms syntactically). Its attractions are real — the kernel becomes
structurally total, the certificate becomes a literally linearized
Foundation equality derivation (finest possible audit granularity),
and no strategy coupling between elaborator and kernel can exist.

It loses on measured grounds: computation-heavy discharges (numeral
tests, unfolding recursive definitions) make traces proportional to
reduction length with β-duplication blowups; the kernel sheds only the
fixpoint driver anyway (single-step application is the same clauses,
and substitution — the genuinely subtle part — stays trusted because
TYPING needs it); and head-exposure traces would spread annotation
plumbing to every elimination position.

The deciding observation: in this theory β was never the dangerous
part of conversion. It is confluent, canonical, owned by the theory's
own ≜ rules, and not user-extensible; the undecidable,
hypothesis-sensitive part of conversion is the EXTENSIONAL part, which
is already certificate-side. β's only sin is potential divergence
under inconsistent hypotheses — a liveness problem, cured by fuel.

Decision: the kernel keeps the ≜-engine, FUEL-BOUNDED, budgets
supplied by the certificate, exhaustion = reject. Totality is
preserved (the verdict gap is closed), certificates stay small (a
number per site, not a computation log), and the audit story is
unchanged (the kernel's normalizer mirrors Foundation's ≜ rules
clause for clause). Full trace-beta remains the documented FALLBACK,
to be revisited only if per-step auditability is ever needed (e.g.
exporting kernel derivations to an external checker) or fuel coupling
bites. The floor in every variant: α-comparison and substitution stay
trusted — no kernel design knows less than that.

REVISITED — see "The derivation rework" below. The rework adopts the
fallback's direction in generalized form: not β-steps in
certificates, but ALL judgements certified, as Foundation-rule
derivation trees — while keeping the fuel-bounded ≜-engine as an
admissible ORACLE rule, which neutralizes exactly the size argument
that made trace-beta lose here. This section stands as the record of
why the oracle exists.

////////// Who produces what: the two tactic layers //////////

The criterion is addressability and persistence.

  * The DISCHARGE ENGINE (built into the elaborator; rewriting,
    whole-equation matching, transitivity hops, quotient witnesses)
    emits CERTIFICATES. Its inputs are core-level equations at sites
    internal to elaboration — there is no surface position its output
    could occupy, by design: the surface has no coercion syntax.
    Certificates are machine-to-kernel format: ephemeral, regenerated
    each run, never authored, never read by the user.
  * AI-LEVEL TACTICS (the AI itself; any future synthesis script) emit
    SURFACE SYNTAX: lemma defs prepended to discharge obligations,
    proof bodies, hints. Obligations — the agent-facing interface —
    are statements, and statements are surface currency.

The boundary is hard: external agents may NOT inject certificate steps.
If the engine finds no trace, the site surfaces as an obligation and
the remedy is a surface lemma that makes the trace findable (typically
turning a search into a one-step direct match). This preserves the
reproducibility invariant:

  THE .nova FILE ALONE DETERMINES ACCEPTANCE.

The elaborator is deterministic, so certificates need no persistence
for correctness — persisting them is a cache/audit policy. Nothing an
agent did that is not in the source file can affect the verdict.

////////// The obligation lifecycle (unchanged) //////////

Discharge failure is never an error: the equation is assumed — as a
CONSTRAINT ENTRY of the run's signature (Foundation: sig-eq/sig-ty-eq;
Σ is OPEN mid-run) — deduplicated, and reported at end of run with its
site and the composite it descended from. The user or AI discharges an
obligation by prepending an ordinary def whose type is the
obligation's statement as an equality type, and re-running. A file is
accepted exactly when the run's final signature is DEFINITIONAL (no
constraints, and — once surface holes land — no declarations) AND the
kernel replays every certificate. Nothing survives between runs. (Full
lifecycle, stratification and metatheory: docs/NovaElaboration.txt.)

////////// Why this shape //////////

The first elaborator implementation placed the discharge engine inside
the trusted boundary: its rewriting and matching were type-blind
(first-order, equation types discarded), and two confirmed exploits
followed — a parametric 𝟙-lemma (x y : 𝟙 ⊢ x ≡ y) whose pattern
matches EVERY equation at EVERY type, certifying Z ≡ S Z ∈ ℕ; and
cross-quotient transport of syntactically identical class equations.
Both are consistency-grade: acceptance authority with no kernel behind
it means an unsound discharge is an unsound acceptance.

The lesson is the LCF lesson: search may not live inside the trusted
boundary. But the opposite pole — no engine, AI-authored proof terms
(J/transp combinators) everywhere, kernel-only checking — was examined
and rejected on measured grounds: transport placement is dense (the
derivation era's coercion ceremony, friction the whole design exists
to eliminate), motives are the most error-prone objects agents write,
statements would inherit the transports, and failures would degrade
from "here is the missing equation" to "your combinator tower is
mistyped." Equality reflection is what makes the middle available:
because every coercion is judgmentally the identity, WHERE the
transport lives is bureaucracy, not semantics — so it can live in a
machine-written, machine-checked, erasure-invisible layer, and the
surface stays exactly as clean as the theory promises.

////////// Status //////////

Implemented: the front end; the elaborator; the obligation lifecycle;
the src/nova/ corpus; the MODULE SYSTEM (file = module, DAG imports
resolved by transitive re-elaboration, qualified names as flat Σ
strings, only accepted modules importable — docs/NovaElaboration.txt,
"Modules"); the EQUATION KERNEL with the demotion of
engine verdicts to proposals — Nova.Kernel provides
fuel-bounded normalization (mirroring the ≜ rules clause for clause,
exhaustion = reject), proof-element inference/checking for elimination
spines and intro forms, injectivity selectors, TYPED PATH DESCENT
(every rewrite's licensed equation is verified against its position's
locally determined expected type — intermediate hops need no type,
congruence only demands the child equation at the rewrite point, and
a type-undetermined rewrite point accepts a neutral subterm at its
own synthesized type, the NEUTRAL-SUBTERM rule of the kernel spec's
§6), and
replay of the certificate finals (beta, el-zero-prop/el-one-prop,
quotient witnesses, el-pi-eta/el-sigma-eta). The discharge engine emits certificates for every discharge
(rewrite traces with parametric-context normalization bridging,
whole-equation matches with condition witnesses, hop chains,
injectivity-selector components), and convElem/convTy count a
discharge ONLY if its certificate replays — a replay failure is
reported on the resulting obligation. Both historical exploits are
golden tests that now END IN OBLIGATIONS (elab-reject-prop-solvent,
elab-reject-cross-quotient): the parametric 𝟙-lemma dies at proof
argument checking, the cross-quotient transport dies at the positional
type check.

The ITEM-LEVEL kernel is implemented: bidirectional re-checking of
whole core items over ANNOTATION SKELETONS — trees positionally
aligned with the core term carrying eliminator motives, expected types
at checked intro forms, switch/refl-eq/well-definedness certificates, and
head-exposure payloads (an expected type whose Π/Σ/quotient structure
only lemma normalization exposes ships as the exposed type plus a type
certificate). The kernel's Σ is the authoritative one: an item is
admitted to it only when the item re-checks from kernel Σ alone, and a
file is accepted exactly when the run's final signature is
definitional and every item was so admitted. Two consequences of that discipline are
load-bearing:

  * certificates carry a TYPE BRIDGE (the equation-level counterpart
    of the exposure payload): a conversion site whose replay steps
    land at positions only a lemma-normalized type determines is
    replayed at that exposed type, justified by a nested TYPE
    certificate — equal types have equal PERs, and a bogus bridge
    dies in replay like any other bad step;
  * a conversion the engine can only close by DECOMPOSING (children
    discharged, but no composite certificate expressible) is assumed,
    not silently accepted — the composite surfaces as an ordinary
    obligation and the remedy is the usual one, a lemma that makes it
    directly matchable. Likewise "clean" is a property of the RUN,
    not the item: an assumption anywhere poisons every later item's
    kernel acceptance (the kernel Σ cannot contain the earlier item,
    so references to it are unresolvable anyway).

Acknowledged approximations, now confined to EQUATION REPLAY (the
item level carries real motives): ℕ-elim z/s rewrite positions and
ℕ-elim proof arguments use the CONSTANT-MOTIVE reading — a valid
congruence/elimination instance whose premises are demanded at the
constant type.

////////// The derivation rework (target architecture) //////////

DECISION (recorded here; design in docs/NovaDerivations.txt): the
kernel is being reworked into a DERIVATION CHECKER — judgements
exactly Foundation's, rules exactly Foundation's, plus two admissible
schemas (presupposition projection; the nf oracle, which is this
pipeline's fuel-bounded ≜-engine wearing a rule's name). The
elaborator-side machinery hands over candidate derivations; the
kernel replays them, rule instance by rule instance, with no
reconstruction of its own.

Why now. The current kernel re-derives judgements from bare core plus
hints, and bare core deliberately loses what derivations have —
eliminator motives above all. Every acknowledged approximation in
docs/NovaKernel.txt (the constant-motive readings, the proof-spine
fragment A4–A6, the typed descent's undetermined positions, the
neutral-subterm rule and its binder-crossing residue) is a negotiated
position on that one frontier, each a trust-bearing reimplementation
of something the elaborator already knows. Checking derivations
deletes the frontier by construction and reduces the kernel audit to
a side-by-side diff against NovaFoundation.txt. The trusted floor is
unchanged: α-comparison, substitution, the ≜-meta-operations, the
normalizer, fuel.

The LCF division sharpens rather than moves: search stays out of the
trusted base, and now RECONSTRUCTION does too. Today's kernel is not
deleted — it crosses the boundary and becomes the RECONSTRUCTOR, an
untrusted component (the discharge engine's kind) that rebuilds
derivations from the current artifacts; its incompleteness becomes a
quality property. The obligation lifecycle, open signatures, module
semantics, the report, and the reproducibility invariant are
untouched — derivations are as ephemeral and regenerable as today's
certificates.

Phasing:

  0. SPEC — docs/NovaDerivations.txt (format: rule-per-node trees
     with no carried spellings — replay is synthesis, every spelling
     delivered by a premise's conclusion; conclusions computed,
     contexts threaded; the delivery discipline; presupposition
     projection; the nf oracle with its load-bearing typing premise;
     the retirement map).                               [DONE]
  1. TRUSTED CORE — the derivation datatype, `conclude`, the two
     admissible schemas, the oracle over the existing normalizer.
     [IN PROGRESS — Nova.Kernel.Derivation checks: the element
     language (formation, typing, equivalence, coercion, reflection,
     congruence, props, normal substitutions, sig references and
     constraints, projection, the oracle); the quotient rules
     including the witness equality and el-quot-eta; the η rules
     el-nat-eta and el-sum-eta (A5 retired at the derivation level);
     ctx formation, σ typing and el/ty-sub-cong-fix; the ν layer
     (poly formation, ty/code-nu, el-nu-i/e, el-nu-coind); and the
     QIIT typing/equality nodes (sort/code instances, constructors,
     the eliminator with COHERENCE EQUALITY DERIVATIONS — A4's
     β-only restriction retired — and el-qiit-path). Exercised by
     the deriv-core golden; the ToS layer is FIRST-CLASS — qctx/qty/
     qtm/qsub one node per Foundation rule, Φ threaded as an input
     in its own conclude family, the ς-calculus reified with its
     action as the meta-operation — so signature formation carries
     no borrowed checking and no A4/A6 residue (ℕ-as-QIIT is formed
     rule by rule in the golden and consumed by the eliminator
     machinery unchanged), and the poly rules are likewise one node
     per rule; the congruence set is complete (eliminator congruences
     with their motive premises — A1's derivation-level retirement —
     let/class/inj/code congruence, sub-norm-ext-cong for spine
     congruence, the full el/ty-sub-cong); the injectivity rules are
     in (ty and code, grouped conclusions split per node); tel/sp
     formation, the ctx/sub/sub-norm/tel/sp equality classes with
     their adopted-once equivalence instances, and the four
     context-coercion rules are in; and the elimination-problem
     classes mot/dalg/eprob/sect are first-class judgements, with
     el-qiit-elim taking an eprob premise and EL-QIIT-ETA replayable
     (the golden replays c ≐ 𝒮.𝕤-elim ℰ · c from an identity section
     candidate; the section-spine meta-operation θ⟨h⟩ generalized to
     arbitrary candidates in Nova.Kernel.QIIT.qSectRhs). Residue,
     all consumer-pending: out/corec congruence (Foundation states
     none — admissible via el-nu-coind), sub-ext/comp-cong and the
     sub-norm context-coercion rules, and the x[e˲]-congruence
     ASSEMBLY (sub-norm-ext-cong + el-sub-cong compose it; phase 2's
     translator will pick the spelling). Phase 2 — the bridge — is
     next.]
  2. BRIDGE — today's kernel instrumented to EMIT derivations
     (becoming the reconstructor); acceptance re-seated on the new
     core. Elaborator, discharge engine and artifact formats
     untouched, so the golden suite and corpus pin the cutover.
     [RE-SEATED — Nova.Kernel.Reconstruct rebuilds derivations from
     the current artifacts (terms + skeletons), and ACCEPTANCE NOW
     SITS ON conclude: where the reconstructor produces derivations,
     an item is accepted iff replay concludes exactly its stated
     judgements — the old kernel does not run at all. Where it
     cannot (the RESIDUE, below), the old kernel's verdict stands
     alone, as before the rework. The road there, slice by slice —
     the shadow phase, during which conclude had to AGREE with the
     old kernel wherever covered, a disagreement a hard error:
     First slice: the structural core with skeleton motives and
     β-only conversion translation (switch/expose → el-ty-coe over
     nf-eq-ty, refl-eq ⋆ → el-eq-i over nf-eq); the whole corpus
     passes with the shadow live, which already caught a conclude
     bug (ty-sig-var concluding its unfolding — ty-sig-beta's
     business). Second slice: type items under the
     shadow, ν formation/intro/observation (poly rebuilt rule by
     rule), and quotient elimination with β-only well-definedness.
     Third slice: the CERTIFICATE
     TRANSLATOR — the retirement map executable: rolling per-side
     trans chains with nf-expand bridges, licensed equations
     reconstructed AT the leaf's context (weakened proof spellings
     re-inferred there, exposed to a literal equality prop by
     projection + the oracle), flips as sym, paths as congruence
     chains with sibling refls, finals mapped (beta → nf-eq, prop
     rules, quotient witnesses, Π-η, the type congruence finals);
     intermediate typings all flow through presupposition
     projections. Fourth slice: the QIIT
     formers — signatures rebuilt through the first-class ToS nodes
     (Φ threaded, embedded pieces checked at their spelled domains),
     sort instances and codes, constructors, and the eliminator with
     its PQCoh coherence certificates through the translator; LPath
     licenses become DQPath leaves. Fifth slice: SELECTOR
     translation — each Sel to its injectivity node, the
     instantiating selectors composed with el-sub-cong-fix, SelSuc
     riding the derivable predecessor route (el-nat-e-cong at the
     constant motive, conjugated by nf-expand). Sixth slice: el-squash-e-prf
     (a rule the derivation core had missed — the reconstructor's
     coverage sweep found it), the FPropExt final, and STRICT MODE —
     NOVA_STRICT_DERIVATIONS=1 makes an uncovered accepted item an
     error, turning the corpus into a coverage meter. Seventh
     through ninth slices: the applied-eliminator fragment (an
     eliminator head applied under a constant-codomain guess), head
     exposure by nf on both the synthesized and the expected side,
     and placement motives read off the expected type. Tenth slice:
     the HYPOTHESIS-SENSITIVE TYPE BRIDGE — a placement at a
     dependent position shifts the equation's type by the step's own
     licensed equation, so the two spellings are walked in parallel
     and the mismatched pair is closed by that very equation
     (both orientations); endpoints whose typing needs the
     hypothesis ride the same bridge; rewriting into the equality
     code's ∈-slot goes through the derived code-eq congruence; and
     a def whose raw type spells solved holes is checked at nf and
     ridden back on the type derivation already in hand. The chain
     type is the certificate's own (tyEx), not the equation's — a
     mixed-spelling trans caught by the shadow, not by the meter.
     Eleventh slice: the ⋆-ENDPOINT ROUTES — an equation endpoint
     that types only at the raw equation type rides the
     certificate's own pre-bridge (tyEx) into the chain type; a bare
     ⋆ at an equality prop closes by reflexivity when the sides
     agree up to nf, else by an assumption of that very equality
     (reflect the hypothesis, reintroduce) — the proof-irrelevance
     shape. Twelfth slice: COINDUCTION — the ⋆-coinduction payload
     replayed through el-nu-coind (whose conclude clause had spelled
     the second ν binder and the closure's 𝔽/R unweakened — fixed
     against Foundation); `out` placements ride Foundation's spine
     route (out ☐₀ over Γ ▷ ν𝔽, the component pushed in by
     sub-ext-cong + el-sub-cong — sub-ext-cong itself a Foundation
     rule the derivation core had missed); and every placement
     parent now squares its child equation's type spelling with the
     congruence's demand by the nf oracle (eqAtNf), which cleared a
     latent el-sum-e-cong disagreement. Thirteenth slice: TYPING
     NORMALIZED SPELLINGS — a bare ⋆ at an equality prop closes by
     nf-reflexivity even when the sides are eliminator-headed, via
     the ENDO guess (an uninferable applied head tried at a → a on
     the argument's own type — the iterated-observation shape nf
     produces) and corec made inferable (it carries its polynomial)
     (currently 13/27 files fully covered; every covered item
     agrees). Fourteenth slice: FORMATION INVERSION (NovaDerivations,
     admissible addition 1½) — el-pi-e's retained codomain premise
     arrives by inv-pi-cod of the head's presupposed Π formation
     when the codomain spells solved holes underivable bare (a
     hole-carrying def APPLIED — cong at stream observations); bare
     re-derivation stays primary so shared-subtree replay cost stays
     linear (inversion re-replays the head, exponential on nested
     spines — an out-of-fuel caught by the shadow). Plus
     NOVA_RECON_DEBUG=1, a permanent first-failure-spine diagnostic
     (currently 15/27; every covered item agrees). Fifteenth slice:
     FORMATION THREADING — the def type's derivation rides down the
     body's lambda spine (inv-pi-cod per binder) and elim-branch
     goals get theirs from the motive derivation substituted along
     the branch's section (ty-sub as cong-fix over refl), so a ⋆
     goal's endpoints arrive by the Prf-equality inversions instead
     of re-typing normalized eliminator spellings bare — sum.nova's
     η law, whose endpoints spell ⊎-elim over an injected variable,
     is the paradigm case (currently 16/27; every covered item
     agrees). Sixteenth slice: the LAST SELECTOR — SelQIdx
     translates to code-qsort-inj-idx (QIIT code injectivity,
     indexwise; Foundation states the QIIT congruence/injectivity
     block structurally, so the node is its indexwise reading), and
     the dual placement — rewriting INTO a QSort index — goes
     through ty-qiit-cong, the same block's congruence reading, with
     sibling indices refl at the left-spine instantiation (currently
     17/27; every covered item agrees). Seventeenth slice: a checked
     LET (the body ⋆'s expose certificate closes the abbreviation
     gap through the let's own hypothesis), and rewriting inside a
     POINT-CONSTRUCTOR SPINE via el-qiit-intro-cong — the structural
     block's congruence reading for QCtor, arguments each at the
     left instantiation, concluding at the left spine's sort
     instance (currently 19/27; every covered item agrees).
     Eighteenth slice: the ELIMINATOR ITEMS — a well-definedness
     equation's endpoint whose typing is its own step license's
     presupposition; the bridges made STEP-OPTIONAL, closing
     mismatched pairs by a SEARCHED path-constructor instantiation
     (candidate spines from the pair's own constructor arguments and
     the context's variables, type-filtered, endpoints verified at
     nf — the quotient's cls-equation under its relation witness) or
     by a context hypothesis of the very equality, reflected; and
     the prop-final close now rides the chain's presupposed
     formation back to the raw spelling (a latent mixed-spelling
     el-trans the shadow caught on the Bag eliminator) (currently
     23/27; every covered item agrees). Nineteenth slice: the path
     search accepts a one-sided constructor pair (a variable against
     pred (suc v) — the truncation path applied under one nesting),
     candidates drawn from nested spines (currently 24/27; every
     covered item agrees). Bails, honestly: the integer tails
     (normalization-heavy endpoint typings) and the vector-append
     tail (an INDEXED motive for a normalized eliminator spelling —
     placements need motive abstraction). Remaining: those bails
     driven down by the strict-mode meter, then the re-seat.
     Twentieth slice: two more endpoint routes — classes of related
     representatives take el-quot-eq with the witness found among
     the context's hypotheses (the genuine-quotient dual of the
     QIIT path search), and an endpoint whose Prf differs from the
     goal's beyond nf rides code-prop-eq (both props HOLD: the
     goal's by nf-reflexivity, the endpoint's own by the endpoint
     itself, weakened — with inv-prf-code delivering the Ω-typing
     from the presupposed formation). Twenty-first slice: quot-elim
     REVISITED — the body goal's formation threaded from the motive
     derivation (its instance along ⟨wk, class ☐₀⟩ by the sub
     calculus), the well-definedness endpoints delivered as
     SUBSTITUTION INSTANCES of the body derivation itself (the
     body's binder sent to ☐₂ by a weakening, to ☐₁ by an extension;
     the ☐₁ instance carried across by code-prop-eq when the motive
     is a prop), the witness final matching its classes raw-first
     and at nf as a fallback with nf-expand bridges, and pair
     components placeable (el-pair-cong). integerAdd and
     integerNormalize cover fully (25/27; every covered item
     agrees). Remaining: eqInt's iff tail, the vector-append tail —
     then the re-seat. Twenty-second slice, toward those tails: a
     step's ∀-lemma RE-INSTANTIATED at searched arguments (candidate
     tuples from the pair's own subterms and the context, filtered
     along the lemma's Π tower or signature telescope — a type
     instance shifts by zeroPlusId at m+k where the step licensed it
     at m); NatElim bridged component-wise (z and index; constant
     motive at the position's type); β-met bridge shapes conjugated
     by the oracle; MOTIVE ABSTRACTION (the expected type with the
     scrutinee's spelling replaced by the fresh binder) as the
     indexed-motive guess at eliminator placements; checked lets.
     Both tails narrow but stand, and with the twenty-third slice
     the seat FLIPS with them documented as residue.

     THE RESIDUE — where the old kernel's verdict still stands
     alone, each with its reason and its exit:

       * ITEMS THE RECONSTRUCTOR DOES NOT COVER. Two corpus files:
         eqInt past eqIIff — argument types spell quot-elim'd
         Ω codes whose well-definedness equations cannot be
         re-derived bare (they need the arithmetic the elaborator's
         certificates carried for OTHER spellings; this is the
         hole-solution shape); and vectByIndAppend past vappendAssoc
         — one lemma-instance step inside an El-vs-unfolding type
         bridge (an index congruence composed with β at the type
         level). NOVA_STRICT_DERIVATIONS=1 remains the meter: it
         turns a residue fallback into an error, so the residue
         cannot silently grow. Exit: phase 3 — the elaborator emits
         the derivation alongside the artifact instead of having it
         reconstructed.
       * HOLE SOLUTIONS — RETIRED IN PRACTICE (phase 3, first
         slice): the mirror now emits each solution's derivations
         and the derivation kernel accepts them;
         kCheckSolution/kCheckTySolution remain only as a fallback
         for solutions emission cannot cover, and nothing in the
         suite or corpus reaches it. A solution neither emitted nor
         old-kernel-checkable is silently not mirrored (references
         stay stuck) — completeness, never soundness.

     The accepted-item invariant, after the seat: for every item the
     reconstructor covers, acceptance = a Foundation-rule derivation
     replayed by conclude to exactly the item's stated judgements;
     for the residue, acceptance = the old kernel's verdict, exactly
     as documented in docs/NovaKernel.txt.]
  3. CONSOLIDATION (incremental, unhurried) — the elaborator emits
     derivations directly where reconstruction is weak or wasteful;
     the trace→derivation translation absorbs the discharge engine's
     certificate format; skeletons retire when nothing consumes
     them. (The end-state has since been revised and strengthened —
     see "Phase 3 end-state, revised" below: the elaborator does not
     merely EMIT derivations, it WORKS IN them.)
     [IN PROGRESS — first slice: the EMISSION BOUNDARY. What was the
     reconstructor is now, in code structure as in trust story, the
     elaborator's derivation-emission pass: the elaborator emits
     (emitDef/emitTyDef, Nova.Kernel.Reconstruct) and hands the seat
     derivations; the trusted side exposes item ACCEPTANCE functions
     (acceptDefItem and kin, Nova.Kernel.Derivation) that replay
     them against the item's stated judgements — the seat composes
     exactly these two. And HOLE SOLUTIONS are seated: the mirror
     emits each solution's derivations (telescope formation
     entrywise, then the type and body in the telescope context —
     solutions carry no skeletons, so emission is bare) and the
     derivation kernel accepts them; the old kernel's
     kCheckSolution remains only as a fallback, which the entire
     suite and corpus no longer exercise (measured: with the
     fallback disabled, 140/140 and 27/27 still pass). The
     hole-solution residue entry below is thereby retired in
     practice; the guessing machinery in the emission pass retires
     guess by guess as later slices record more at elaboration
     time. Second slice, at the two def-item tails: branch checks of
     an indexed-motive placement descend lambdas and bridge at the
     leaf with the step's own lemma (chkStep); motive abstraction
     keys on either side of the rewrite (the expected type is the
     motive instance at whichever spelling the equation's type
     already speaks); a subterm-rewrite proposal (replE) lets a
     bridge cross an index rewrite that changes a code's shape only
     after β; and the ∀-lemma instantiation search is SCOPED — leaf
     sites and the per-step shift sites only, never the ambient
     bridge, after an ambient version sent covered items into
     minutes-long search storms (the whole suite now runs in ~35s,
     the corpus meter in ~11s, and anything past ~15s per item is
     treated as a blowup). vappendAssoc's nil branch now closes; its
     successor branch stands one wall further (the ih application
     under the abstracted motive), and eqIIff stands unchanged —
     both remain the residue, their exit the engine-side emission
     this phase is for. Third slice: the CERTIFICATE FORMAT
     ABSORBED — formations ride the certificate's own structure
     instead of bare re-derivation, everywhere. reEqTy takes
     optional endpoint formations (a switch's inferred side by
     presupposition, an expose's checked side likewise); a side
     untouched by steps closes ONE-SIDED, by the chain's own
     β-normal end — its formation arriving free as the conclusion's
     presupposition, in both orientations; the tyEx pre-bridge
     REPORTS WHERE IT REACHED and the equation carries on at the
     reached spelling; the type-congruence finals hand their
     component equations inversion-projected formations; the
     placement walk THREADS the chain's presupposed typing down
     through the formers (inv-el-code, inv-prf-code, and the
     code-eq typing inversions inv-code-eq-lhs/rhs/ty — subject-
     directed, so type conversion cannot interfere); and the
     formation-threaded checker covers switch sites and application
     arguments (the domain by inv-pi-dom of the head's presupposed
     Π). eqInt COVERS FULLY — the eqIIff tail falls (26/27; every
     covered item agrees). vectByIndAppend alone remains. Fourth
     slice — a finding worth its cost: the planned "engine-side
     certificate enrichment" rests on a FALSE premise. The engine's
     replay never forms the shifted instance types at all — it
     rewrites spellings and compares normal forms at the one fixed
     equation type — so there is no induced type chain for it to
     record; the shifts are artifacts of the DERIVATION-side
     congruence conclusions (el-app-cong concludes at B[e′], the
     chain speaks B[e]). A store-lemma instantiation search at the
     shift sites was built and REVERTED: typed enumeration times a
     hundred retry attempts is a budget storm, and its proposals
     drift into junk spellings (kept: the step's own lemma
     re-instantiated, tag-gated and pool-filtered — cheap and
     precise). The principled exit is search-free and derivation-
     side: the PLACEMENT builds its own pre-instance bridge from the
     child equation it already holds — sub-ext-cong on ⟨id, child⟩,
     then ty-sub-cong at the fixed family — composed level by level
     up the placement walk. Sixth slice, explored and REVERTED whole —
     recorded because the measurements matter: RAW-FIRST STEP
     PLACEMENT works. A step placed at the raw spelling — the
     licensed occurrence found up to nf through solved-hole
     references, the intended occurrence disambiguated by the old
     kernel's own step replayer, the two spellings joined by a lean
     one-difference walk — bypasses the β-created towers entirely,
     and raw placement failures fell from 58 to 4 on the residue
     file. But the unlocked progress expanded the downstream
     attempt cascade, whose ambient bridge costs (leaf searches
     times roughly a hundred retry attempts) ran to millions of
     normalizations; even a per-item normal-form cache left the
     file an order of magnitude over budget. The prerequisite is
     MEMOIZED EMISSION: the retry structure re-solves identical
     subproblems wholesale, and no per-site search discipline fixes
     that — an infrastructure slice (memo tables or a single-pass
     attempt discipline), after which the raw-first chains become
     affordable. Seventh slice: MEMOIZED EMISSION, landed — memo
     tables over the skeleton-free entry points (checking,
     inference, formation, and both normalizers), keyed by the
     spellings' structural Show output with the context included;
     FAILURES are cached too, which is what stops the retry storms;
     the tables clear at each emission entry (Σ is fixed within one
     item — and the clear must be a real function, not a top-level
     constant: a CAF clears once ever, and the stale tables that
     resulted cost four covered files before the meter caught it).
     Untrusted side only — replay never sees a cache. The residue
     file's attempt tree drops from minutes to ~4s; covered files
     pay a modest key-construction overhead. The raw-first
     placement was then re-applied on the memoized base and STILL
     exceeds budget — with duplicates eliminated, its cost is
     DISTINCT work: every variant spelling makes each downstream
     sub-problem new, so no cache helps. The next lever is pruning
     the attempt tree itself (translating each ⋆'s certificate once
     and sharing it across the body-level attempts), not caching.
     Fifth slice: that design LANDED at its
     three shift-introducing cases — el-app-cong's argument
     position, the second projection, and the ℕ-elim scrutinee each
     coerce their conclusion back to the PRE-instance by their own
     child equation (sub-ext-cong on ⟨id, sym child⟩, ty-sub-cong at
     the fixed family), so the surrounding chain sees no dependent
     shift at all. vectByIndAppend still stands — beneath the chain
     shifts, its successor branch's ih application under the
     abstracted motive wants composites of index laws at spellings
     only the original elaboration knew; that is phase 3's full
     end-state (the elaborator emitting the induction's derivations
     at its own ⋆s), not another reconstruction guess. Eighth
     slice: BETA-AT AND THE POSITIONAL ROUTE, landed — the kernel
     gains the single-≜-step rule (docs/NovaDerivations.txt,
     addition 2½: one contraction at a path inside a typed term, no
     subterm typing premise, by subject reduction) plus the
     machinery under it (a one-step contractor per ≜ rule, path
     readers/writers, an LBeta step license); when a certificate's
     legacy replay fails at a ⋆ goal, the emission RE-EXPRESSES its
     chain against the goal's spellings as they arrive: each
     licensed side is SEARCHED for as a subterm — matching against
     the license's STATEMENT spelling (licensedRaw, nothing
     normalized), ≜-contracting goal or license only where the match
     demands (a two-sided meet), every goal contraction recorded as
     a beta-at link, the lemma path rebased to the found position —
     and the re-expressed chain replays with placements on the raw
     spellings (no motive guesses), a compact rolling typing witness
     (embedding the whole chain in each link's premise replays
     exponentially), and the type-expansion skipped (positional
     steps are re-expressed at the raw type; the recorded expansion
     belongs to the legacy chain). Landed with it: memoized rescue
     and bridge-search tables, size gates on every search that
     walks normalized spellings, and a match-directed instantiation
     pool (an instance's endpoints must BE the pair, so its
     arguments occur in it — blind enumeration buries the right
     tuple beyond any affordable cap). This clears the z-branch ⋆
     and every index-law equation whose licensed spelling meets its
     goal; the successor branch still stands on its LAST gap, now
     precisely one: a placement's DEPENDENT SHIFT whose two nf'd
     vect indexes differ by an index-law COMPOSITE (one instance
     per side, e.g. plus-assoc against zero-id), and the
     instance-meet bridge for that pair (lemBridgeT, present,
     env-gated NOVA_LEMBR) still exceeds budget on its enumeration.
     The one-file residue holds at 26/27, and the residue file's
     full attempt tree now runs in ~1s. Ninth slice (the stopgap,
     partial): the instance-meet bridge is rebuilt on RECORDED
     instances — the pool's steps are fully instantiated licenses,
     so matching is exact comparison and placement is direct code
     congruence (constant motives, no search); the successor
     branch's dependent-shift pair now CLOSES, and the enumeration
     it replaces is deleted. What stands after it is one level
     deeper — the S-instantiated branch re-check, whose spellings
     explode under exposure — and rather than another wall-by-wall
     chase the emission gains a per-item WORK BUDGET (one countdown
     over every reconstruction entry; exhaustion is residue in
     bounded time, never a hang) plus structural-hash memo keys
     (double-hash fingerprints over spellings, ToS signatures and
     polynomials included — Show-string keys were a measured
     memmove storm, and structural == on motive-bearing contexts a
     measured walk storm). The meter drops to ~14s, the suite to
     ~40s, and no input can hold the emission for minutes again.
     The residue holds at 26/27; the recorded-instance bridge is
     evidence FOR the revised end-state rather than a route around
     it — the instances it consumes are exactly the witnesses
     judgment-carrying elaboration would have in hand.]

    [Tenth slice, the DERIVATION WALK: placement machinery reads its
     premises off a rolling structural witness instead of
     reconstructing them. The chain carries, beside the compact
     presupposition witness, a STRUCTURAL one: seeded per side (the
     by-term store, else one plain inference under its own
     allowance), transformed per exposure by constructive subject
     reduction (stepTypedE: one ≜ contraction ON the derivation —
     β from the λ's own premises, ℕ-elim from the branches, SigVar
     unfold by substituting into the recorded body derivation), and
     read by rePlaceE's ZIPPER routes (Π-app both slots, pair
     second slot, ℕ-elim scrutinee — the MOTIVE is a premise read,
     never a guess). Premise access as projection arrives early:
     dvView projects a side's typing out of the equation's own
     congruence skeleton (presup-of-cong views, presup-of-beta-at
     via the walk), so the placement's own conclusion re-seeds the
     witness after each rewrite. Accepted items record their body
     derivations by name (share-free re-emission for small bodies —
     a DShare citation cannot be walked — plus an on-demand lazy
     record for residue-accepted solutions). Around the walk, the
     bridge machinery gained what the pool recordings cannot give:
     nf'd variants of recorded instances, ∀-re-instantiation by
     first-order MATCHING of a lemma's nf'd statement sides against
     the pair (enumeration cannot reach a three-argument tuple),
     closed SIGNATURE laws as instance sources (no recorded-context
     skew), and one-sided rewrites at any occurrence within any
     disagreement prefix, backtracking across candidates under the
     fuel bound and the memo. Result: the S-instance's FIRST
     placement — the eighth slice's wall — goes through end to end,
     bridge included. What stands is one level deeper again: the
     SECOND placement's witness view crosses a position the first
     rewrite shifted, where the β-substitution inside the walk
     needs the propositional index bridge itself — the walk's
     typing-rule exactness meets the same shift the equation rules
     absorb in their conclusion instances. The residue holds at
     26/27 (suite 140/140, meter ~35s); every mechanism above is
     permanent judgment-carrying substance, not stopgap — the
     witness IS the Jud the end-state elaborator will carry. A
     follow-up adds the THREE-WAY FIT (fitEntry/fitNode): a stepped
     or spliced premise coerced to the exact spelling its enclosing
     node demands — identical spellings pass, nf-equal ones ride
     nf-eq, a REAL index shift rides the license pool through
     lemBridgeT with the ambient context threaded (dropped at
     binders). The El-vs-former leg of that gap now closes (the
     El-vs-former clause gained the nf'd and match-proposed
     instances the El/El clause already had, plus its mirror
     orientation), and lemBridgeT walks Π/Σ towers component-wise
     (structural descent preserving the rewrite-chain fuel, refl
     where a component already agrees, instances under binders
     supplied by the context-robust signature-law matcher after the
     empty-recordings bail learned to defer to proposals). A further
     round closed the composition too: the empty-recordings bail
     defers to the signature-law matcher on SMALL pairs (the
     under-binder components where recordings cannot instantiate —
     the license pool is also restored across nested replays now,
     as the witness already was), the bridge carries the endpoints'
     FORMATIONS down the Π/Σ descent (a refl leg must not re-derive
     a normalized eliminator code's formation — the motive-guessing
     wall, met again one level down), and the walk's defensive
     concludes run at a separate VALIDATION FUEL with fuel-death a
     soft verdict (a fitted composite can out-fuel a check without
     being wrong; the megastep attempts were a measured 3x on the
     meter). With all of that the S-case chain WALKS END TO END —
     every exposure and both placements' witnesses. What remains is
     the second placement's leaf: its licensed spelling meets the
     chain's at a type whose nf blows past every allowance, the
     last un-walked reconstruction in the file. The corner is
     exactly the class of reconciliation the judgment-carrying port
     dissolves: a witness carried from birth is spelled right by
     construction.]

////////// Phase 3 end-state, revised: judgment-carrying elaboration //////////

The eighth slice's last gap (the dependent-shift composite) forced
the question the phasing had deferred: WHERE do derivations come
from? Every formulation so far — reconstruct at the seat, enrich the
certificate, emit at the ⋆s — is a way of inverting an erasure after
the fact. The revised end-state stops erasing instead:

  THE ELABORATOR WORKS IN DERIVATIONS. Its working state is not the
  triple (context, element, type) but a derivation concluding that
  judgement; the triple is the derivation's ERASURE, cached for
  matching and printing. Erasure loses data by design — that is what
  makes it cheap — and every phase-3 difficulty to date has been the
  cost of inverting it: motive reconstruction, shift witnesses, the
  positional meet, each a fiber of a projection that is not
  injective. Computed upstairs, there is nothing to invert.

The theory already says this. NovaFoundation's official reading is
intrinsic, and NovaSyntax makes it a QIIR: in that reading a term IS
its typing derivation, quotiented by the equations. A derivation
value is a point of the presentation before quotienting; erasure is
the quotient map; conclude checks that a candidate point coheres.
Judgment-carrying elaboration is the implementation converging on
the spec's own semantics — today's elaborator computes in the image
of the projection, the revised one computes in the presentation.

The commitments, each load-bearing:

  * NO NAKED SPELLINGS. Every term the elaborator holds arrives with
    its derivation. Premise access — the typing of a subterm, the
    motive of an eliminator, the domain behind an application — is
    PROJECTION through the derivation's administrative nodes
    (coercions, substitution wraps), a deterministic unfold with no
    choice points; never INVERSION, which in an extensional theory
    is a meta-theorem that conversion makes false or hard. The
    presupposition and projection nodes already in the derivation
    language let most of it stay there, deferred to replay.

  * SUBSTITUTION WRAPS. The admissible substitution lemma is not run
    at every application node; the calculus has explicit
    substitutions and sub-congruence rules, so substitution on a
    derivation is a CONSTRUCTOR — one node, O(1) — and the executable
    admissible lemma pushes wraps down only on demand, one clause at
    a time, during projection.

  * COMPUTATION STEPS ARE EQUATIONS. A normalization the proof will
    descend into is a traced beta-at chain (each contraction one
    premise-free link; the right presupposition delivers the new
    spelling's judgement); a normalization it will not descend into
    is one nf-oracle node. Contraction-born spellings never travel
    naked, so the class of problems they caused does not exist here.

  * REWRITING IS DEMOTED FROM MECHANISM TO COROLLARY. There are no
    paths, no splices, no positional type reading, and the
    certificate format has no successor. What a rewrite used to do is
    expressed natively: trans, congruence frames (congruence with
    refl in the unchanged children), a lemma instance at the root of
    a subgoal — built FORWARD, premises in hand, by a refiner over
    equation goals. convElem's decompose is already this shape at the
    outer layer; the revision extends it all the way down and deletes
    the positional core beneath it.

  * SEARCH IS ERASED AND UNPRIVILEGED. Undecidability means the
    intermediates of a trans chain and the choice of lemma instances
    remain a search; the nf-and-meet rewriter survives, if at all, as
    a heuristic over erasures that PROPOSES plans. The refiner
    constructs from proposals with the derivation in hand, so the
    information gap between finder and justifier — the defining
    failure mode of the reconstruction era — cannot open. A proposal
    that does not construct is a failed proposal, not residue.

  * HOLES STAY IN Σ. A hole is a signature declaration, its solution
    a later item; derivations reference holes by name and replay
    against the final signature. Nothing ever substitutes into a
    stored derivation.

  * THE TRUST STORY DOES NOT MOVE. Derivations are untrusted data;
    the seat erases and replays through conclude, which remains the
    sole authority. Derivation hygiene (collapsing coercions by refl,
    fusing trans chains) rewrites only along admissible steps, so the
    artifact stays a complete, walkable record — no data lost.

Migration is strangler-fig, inside the existing elaborator, under
the existing meters: the construction spine of checking and
inference ports route by route to judgment-carrying form; the
emission pass (Nova.Kernel.Reconstruct) is demoted to the ADAPTER
that lifts still-erased routes to derivations, falling to the old
kernel where it cannot; the strict meter then measures the port —
covered-by-construction against covered-by-adapter against residue.
One stopgap precedes it (recording the dependent-shift witnesses in
today's certificates, closing the meter at 27/27) and is deleted
with the rest. At the end, the reconstruction pass, the certificate
format, and the old kernel's equation replay are deleted; the cost
model collapses from three walks to one.

Cost, stated honestly: until phase 3 completes, an item is walked
three times (elaborate, reconstruct, replay) — a constant factor,
bought back by deleting the reconstruction frontier from the trusted
base and every A-caveat with it.
