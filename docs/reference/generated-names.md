# Generated names
%stub

What the item macros put into scope, and how to cite it.

## From a `data` item

- Sorts, constructors, `<Sort>Elim`, `<Sort>ElimP`.
- `<name>.eq`, `<name>.unfold`, and what each licenses.

## From a clausal `def`

- The definition itself, one equation lemma per clause, the uniqueness
  lemma.
- Default names and the `[name]` overrides.

## Citing them in `using`

- Qualified versus unqualified spelling.

## The special sources

- `hyp.rw` and the hypothesis store.
